CREATE TABLE Member (
                        userId varchar(60) primary key,
                        password varchar(100),
                        name varchar(60),
                        email varchar(60)
);