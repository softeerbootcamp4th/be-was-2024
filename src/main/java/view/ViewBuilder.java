package view;

import java.util.Map;

public class ViewBuilder {
    // original view를 적절한 형태로 변환한다.
    public String build(String originalView, Map<String, Object> items) {
        StringBuilder builder = new StringBuilder();

        return builder.toString();
    }
}
